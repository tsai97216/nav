name: Version Check

on:
  push:
  pull_request:

jobs:
  verify-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify NAV version references
        run: python scripts/verify-version.py
